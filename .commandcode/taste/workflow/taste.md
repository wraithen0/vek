# Workflow Preferences

## Task Execution
- Prefers deploying sub-agents for parallel/independent fix work after issues are identified — explicitly requests "deploy sub-agents" rather than sequential fixing. Confidence: 0.85
- Wants identified issues to be fixed, not just documented — expects action after analysis. Confidence: 0.8
- Prefers systematic, prioritized task tracking (e.g., ranked todo lists with clear status) when handling multiple fixes. Confidence: 0.7
- Prefers a single, unified build pipeline over multiple parallel build systems — dislikes redundant build tooling (e.g., Makefile + CMake + setup.py doing overlapping work). Confidence: 0.85
